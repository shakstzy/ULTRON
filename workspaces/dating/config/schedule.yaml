# dating — declared launchd cadence

workspace: dating

workspace_jobs:
  lint:
    cron: "0 4 * * *"  # Daily 04:00

# Tinder bot plists live at _shell/plists/com.adithya.ultron.dating-tinder-{swipe,pull,send}.plist.
# Hand-written (not generated by the schedule skill) because the bot's irregular send schedule
# (8 fires at non-uniform minutes) doesn't fit run-stage.sh's stage model. The skill will report
# them as orphans on `compile`; that's expected.
#
# Cadence:
#   swipe: 12:30 + 21:00 CST, 0-30min jitter
#   pull:  15:00 + 23:00 CST, runs pull then decide, 0-30min jitter
#   send:  9:23, 11:18, 12:47, 14:11, 16:39, 18:24, 20:46, 22:38 CST, 0-30min jitter

# audit, weekly-review, graphify-supermerge live in _shell/config/global-schedule.yaml.
