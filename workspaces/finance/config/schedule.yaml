# Finance — declared launchd cadence. Conservative defaults; Adithya tunes
# after wiring real Plaid / brokerage feeds.

workspace: finance

sources:
  manual:
    cron: "0 4 * * *"

# Gmail intentionally left out — sources.yaml has `gmail: []` so nothing
# routes here yet. Wire up later once finance-relevant filters are scoped.
