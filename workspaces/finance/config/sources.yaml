# Phase 3 starter: Gmail block empty pending schema extraction.

workspace: finance
wiki: true

sources:
  gmail: []           # Pending schema extraction — finance-relevant Gmail rules unknown.

  plaid-export:
    watch_path: raw/plaid/_inbox/
    after_ingest: move_to_dated_subdir

  manual:
    watch_path: raw/manual/_inbox/
    after_ingest: move_to_dated_subdir

global_settings:
  attachments_inline: true
  vision_model: sonnet
  hash_algorithm: blake3
