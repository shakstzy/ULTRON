# Phase 3 starter: Gmail block empty pending schema extraction.

workspace: health
wiki: true

sources:
  manual:
    watch_path: raw/manual/_inbox/
    after_ingest: move_to_dated_subdir

  apple-health:
    watch_path: raw/apple-health/_inbox/
    after_ingest: move_to_dated_subdir

  gmail: []   # Pending schema extraction — health-relevant Gmail rules unknown.

global_settings:
  attachments_inline: true
  vision_model: sonnet
  hash_algorithm: blake3
