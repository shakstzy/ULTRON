# library — declared launchd cadence

workspace: library

# Manual deposits process at lint time. Defuddle / docling / youtube ingest
# wired when those ULTRON-local skills land.

# audit, weekly-review, graphify-supermerge live in _shell/config/global-schedule.yaml.
