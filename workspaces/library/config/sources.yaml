# library — declared ingestion sources
# Cross-source routing rules in _shell/docs/source-routing.md.

workspace: library
wiki: true

_workspace_meta:
  ingest_unrouted_default: skip

sources:
  manual:
    watch_path: raw/manual/_inbox/
    after_ingest: move_to_dated_subdir
  # defuddle article extracts, youtube transcript ingest, docling PDF parse
  # added as the ULTRON-local copies of those skills are wired.

global_settings:
  attachments_inline: true
  vision_model: sonnet
  hash_algorithm: blake3
