# <workspace> — declared ingestion sources

workspace: <workspace-slug>
wiki: true   # set false for pure workflow workspaces

sources: []
# Example sources — uncomment and adapt at bootstrap:
#
#  - id: gmail-<workspace>
#    type: gmail
#    bin: ingest-gmail.py
#    config:
#      account: <email>
#      labels: ["Inbox"]
#      exclude_labels: ["Promotions", "Spam"]
#      lookback_days: 1
#      lookback_days_initial: 365
#    output_path: raw/gmail/{YYYY-MM}/
#
#  - id: slack-<workspace>
#    type: slack
#    bin: ingest-slack.py
#    config:
#      workspace_id: T0XXXXXX
#      channels: []
#      include_dms_with: []
#      lookback_hours: 24
#    output_path: raw/slack/{YYYY-MM}/

global_settings:
  attachments_inline: true
  vision_model: sonnet
  hash_algorithm: blake3
