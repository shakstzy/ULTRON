# Personal — declared launchd cadence.

workspace: personal

sources:
  gmail:
    cron: "0 */3 * * *"              # Every 3 hours (less aggressive than work)
    accounts:
      - adithya.shak.kumar@gmail.com
  imessage:
    cron: "0 */6 * * *"              # Every 6 hours; chat.db is local
  whatsapp:
    cron: "*/30 * * * *"             # Every 30 min; bridge SQLite is local + scan is sub-second
    accounts:
      - "+15126601911"

workspace_jobs:
  lint:
    cron: "10 4 * * *"               # Daily 04:10
  graphify:
    cron: "0 */6 * * *"
