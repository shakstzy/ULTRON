# property-management — declared ingestion sources
# Cross-source routing rules in _shell/docs/source-routing.md.

workspace: property-management
wiki: true

_workspace_meta:
  ingest_unrouted_default: skip

sources:
  gmail:
    accounts: []   # Populated from source-routing.md.
  manual:
    watch_path: raw/manual/_inbox/
    after_ingest: move_to_dated_subdir
  # zillow rental manager browser scrape added once that skill is ported
  # from SHAKOS into ULTRON-local form.

global_settings:
  attachments_inline: true
  vision_model: sonnet
  hash_algorithm: blake3
