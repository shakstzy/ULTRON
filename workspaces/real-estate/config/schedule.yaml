# real-estate — declared launchd cadence

workspace: real-estate

# No source-driven ingest crons until source-routing.md is populated.

workspace_jobs:
  lint:
    cron: "0 4 * * *"  # Daily 04:00

# audit, weekly-review, graphify-supermerge live in _shell/config/global-schedule.yaml.
