# sei — declared launchd cadence
# Past-job archive workspace; no active ingest crons.

workspace: sei

# audit, weekly-review, graphify-supermerge live in _shell/config/global-schedule.yaml.

workspace_jobs:
  lint:
    cron: "2 4 * * *"
  graphify:
    cron: "0 */6 * * *"
