# trading — declared launchd cadence

workspace: trading

# No source-driven ingest crons until arbitrage-detection pattern ported
# from SHAKOS. Manual entries process at lint time.

# audit, weekly-review, graphify-supermerge live in _shell/config/global-schedule.yaml.
