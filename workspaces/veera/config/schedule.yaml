# veera — declared launchd cadence
# Past-contract archive workspace; no active ingest crons.

workspace: veera

# audit, weekly-review, graphify-supermerge live in _shell/config/global-schedule.yaml.

workspace_jobs:
  lint:
    cron: "15 4 * * *"
  graphify:
    cron: "0 */6 * * *"
